---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for page in site.pages %}
    {% unless page.exclude %}
  <url>
    <loc>{{ site.url }}{{ page.url | relative_url }}</loc>
    <lastmod>{{ page.last_modified_at | date_to_xmlschema | default: site.time | date_to_xmlschema }}</lastmod>
    <changefreq>{% if page.changefreq %}{{ page.changefreq }}{% else %}monthly{% endif %}</changefreq>
    <priority>{% if page.priority %}{{ page.priority }}{% else %}0.5{% endif %}</priority>
  </url>
    {% endunless %}
  {% endfor %}
  {% for post in site.posts %}
  <url>
    <loc>{{ site.url }}{{ post.url | relative_url }}</loc>
    <lastmod>{{ post.last_modified_at | date_to_xmlschema | default: post.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>
  {% endfor %}
  {% for project in site.projects %}
  <url>
    <loc>{{ site.url }}{{ project.url | relative_url }}</loc>
    <lastmod>{{ project.last_modified_at | date_to_xmlschema | default: project.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
  {% endfor %}
</urlset>
